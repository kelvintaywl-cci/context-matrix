version: 2.1

jobs:
  quote:
    docker:
      - image: cimg/base:stable
    parameters:
      ctx:
        type: string
        default: ''
        description: 'placeholder/hack; do not set'
    steps:
      - checkout
      - run: |
          echo "TODO: random quote"

workflows:
  explore:
    jobs:
      - quote:
          context: << matrix.ctx >>
          matrix:
            parameters:
              ctx:
                - "test-2"
                - "test-3"
                - "test-4"
